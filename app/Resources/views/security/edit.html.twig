{% extends 'base.html.twig' %}

{% block body %}

    <h1>Vos informations</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-primary" value="Modifier">
    {{ form_end(form) }}

{% endblock %}