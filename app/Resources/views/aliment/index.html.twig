{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste des aliments</h1>

    <table class="table table-hover">
        <tbody>
        {% for aliment in aliments %}
            <tr>
                <td><a href="{{ path('aliment_show', { 'id': aliment.id }) }}">{{ aliment }}</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
