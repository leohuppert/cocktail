{% extends 'base.html.twig' %}

{% block body %}
    <h1>Aliment</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aliment.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ aliment.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('aliment_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('aliment_edit', { 'id': aliment.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
